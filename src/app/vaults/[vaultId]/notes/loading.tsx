import React from 'react';
import {CircularProgress} from "@mui/joy";

const Loading = () => {
    return (
        <main>
            <CircularProgress/>
        </main>
    );
};

export default Loading;