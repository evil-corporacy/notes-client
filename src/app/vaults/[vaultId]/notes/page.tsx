import React from 'react';
import {checkAuth} from "@/features/check-auth";

const Page = () => {
    checkAuth()

    return (
        <div>
            
        </div>
    );
};

export default Page;