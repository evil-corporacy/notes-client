export interface User {
    nickname: string
    avatarURL: string
    mail: string
    password: string
}

export interface Note {
    id: string
    imageUrl: string
    title: string
    content: string
}